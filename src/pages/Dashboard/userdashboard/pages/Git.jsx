import React from 'react';
import { Routes, Route } from "react-router-dom";
const Git = () => {
    return (
        <React.Fragment>
            <section>
                <div className="centered text-4xl h-screen"><h1>This is Git Page</h1></div>
            </section>
        </React.Fragment>
    );
};

export default Git;
